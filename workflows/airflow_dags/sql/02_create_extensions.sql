-- Create database extensions for ShopZada DWH
-- This script sets up necessary PostgreSQL extensions

-- Enable PostGIS for geospatial data (if needed)
-- CREATE EXTENSION IF NOT EXISTS postgis;

-- Enable other useful extensions
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Log completion
SELECT 'Database extensions created successfully' as message;
